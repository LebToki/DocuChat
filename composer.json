{
  "name": "ttarabichi/docuchat",
  "description": "A local PDF chatbot using PHP for the frontend and Python for LLM and embeddings.",
  "require": {
    "guzzlehttp/guzzle": "^7.0"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  },
  "config": {
    "optimize-autoloader": true,
    "sort-packages": true
  }
}
